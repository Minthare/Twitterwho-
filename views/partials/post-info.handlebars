<div class="card mt-3">
    <div class="card-header bg-light text-info">
       <h4><a class="link text-info" href="/post/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <p class="card-body">{{content}}</p>
</div>